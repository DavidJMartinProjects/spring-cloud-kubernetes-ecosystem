import { LeaderboardResponse } from './leaderboard-response.model';

describe('LeaderboardResponse', () => {
  it('should create an instance', () => {
    expect(new LeaderboardResponse()).toBeTruthy();
  });
});
